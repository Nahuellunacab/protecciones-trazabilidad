# Perfil de desarrollo (default, activo también en docker-compose.override.yml).

# Loguear el SQL generado por Hibernate ayuda a depurar consultas en desarrollo.
spring.jpa.show-sql=${JPA_SHOW_SQL:true}

logging.level.protecciones=DEBUG
